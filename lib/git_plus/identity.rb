# frozen_string_literal: true

module GitPlus
  # Gem identity information.
  module Identity
    NAME = "git_plus"
    LABEL = "Git Plus"
    VERSION = "0.1.0"
    VERSION_LABEL = "#{LABEL} #{VERSION}"
  end
end
