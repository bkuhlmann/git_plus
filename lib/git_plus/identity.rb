# frozen_string_literal: true

module GitPlus
  # Gem identity information.
  module Identity
    NAME = "git_plus"
    LABEL = "Git+"
    VERSION = "0.2.0"
    VERSION_LABEL = "#{LABEL} #{VERSION}"
  end
end
